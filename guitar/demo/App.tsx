import { Button } from '@guitar/components/button'

export default function App() {
  return (
    <div style={{ padding: '2rem', display: 'flex', gap: '1rem' }}>
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
    </div>
  )
}
